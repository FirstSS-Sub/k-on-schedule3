import React from "react";
import ResponsiveDrawer from "../templates/ResponsiveDrawer";

const Home: React.FC = () => {
    return (
        <ResponsiveDrawer>
            <>ホームページの内容</>
        </ResponsiveDrawer>
    );
};

export default Home;
